# 시장 신호등 자동 모니터링 시스템 — 개발 지시문 v2 (풀 디벨롭판)

> 이 문서를 Claude Code에게 그대로 전달하세요. 처음부터 끝까지 **단계별로** 작업하면 됩니다.
> 작성자는 코딩을 잘 모르는 사용자입니다. 각 단계마다 무엇을 하는지 쉽게 설명하고, 사용자가 직접 해야 할 일(키 발급·버튼 클릭 등)은 **클릭 단위 가이드**로 안내하세요.

---

## ★ v2 변경 요약 (원본 대비 무엇이 바뀌었나)

원본(v1)의 좋은 골격 — 단계별 진행, 클릭 가이드, `config` 분리, 면책 조항, 비개발자 친화 톤 — 은 **그대로 계승**합니다. 아래는 검토 의견서를 반영해 보강한 핵심 변경입니다.

| # | 영역 | v1 | v2 (변경/추가) | 이유 |
|---|------|----|----|------|
| 1 | **VIX 소스** | Yahoo `^VIX` | **FRED `VIXCLS`로 이전** | Yahoo는 클라우드(깃허브) IP에서 자주 차단됨. VIX는 FRED로 안정적 수급 가능 |
| 2 | **WTI 소스** | Yahoo `CL=F` | **FRED `DCOILWTICO` 1순위 + Yahoo `CL=F` 폴백** (아래 "WTI 확인 결과" 참조) | Yahoo 의존을 더 줄임. 단 FRED는 며칠 지연(lag) 있어 신선도 검증 동반 |
| 3 | **잔여 Yahoo 지표** | 그냥 호출 | HYG·KRE는 **재시도(retry)+지수 백오프+User-Agent 설정+실패 시 결측 처리** | Yahoo 일시 차단(429)은 잠깐 기다리면 풀리는 경우 많음 |
| 4 | **무음 실패 방지** | 없음 | **heartbeat(정상 작동 통지) + 실패 시 텔레그램 에러 알림(`if: failure()`)** | "조용히 죽는" 모니터링은 없느니만 못함. 알림 안 오는데 안전한 줄 오인 방지 |
| 5 | **종합 판정** | 4단계 | **"판정 보류(회색)" 추가 + yellow 다수 보조경고 + 단기패닉 옵션** | 결측 다수를 🟢로 오판정하는 함정 차단 |
| 6 | **GitHub Actions** | 1단계 | **7단계로 세분화 + `workflow_dispatch`(수동 버튼) + cron은 맨 마지막** | 비개발자 최대 난관. cron 기다림 없이 즉시 테스트 |
| 7 | **타임존** | 암묵적 | **`util.py`로 ET/KST/UTC 일원화, naive datetime 금지, KST↔UTC 환산표** | 날짜 비교 버그 온상 차단 |
| 8 | **로깅·테스트** | print | **`logging` 모듈 + `signals.py` 단위 테스트 최소 1개(`tests/`)** | 실패 추적 + 임계값 경계 검증 |
| 9 | **로그 저장** | Actions 커밋 누적 | **`permissions: contents: write` + rebase 후 push** (권장안 명시) | push 충돌·권한·히스토리 오염 방지 |
| 10 | **★ 웹 대시보드** | 없음(여지만) | **GitHub Pages 무료 정적 대시보드(`docs/`) + 과거 추이 라인 차트(Chart.js)** | 폰·PC 어디서나 URL로 신호등을 눈으로 확인 |
| 11 | **지표 메타데이터** | 임계값만 config | **소스·코드·tier·방향·임계값을 한 테이블로 강화** | 지표 추가/삭제를 config 한 곳에서 |

> **WTI FRED 대체 — 확인 결과(2026-06-04 웹 확인):** FRED `DCOILWTICO`(West Texas Intermediate, Cushing OK)는 **실제 존재하는 일별(daily) 시리즈**이며 출처는 미 에너지정보청(EIA)입니다. 다만 실시간 선물(`CL=F`)과 달리 **며칠 지연(lag)이 있을 수 있습니다.** 따라서 v2는 **WTI를 `DCOILWTICO` 1순위로 쓰되, 받은 값의 날짜가 오래됐으면(신선도 검증) Yahoo `CL=F`로 폴백**하는 전략을 채택합니다. 둘 다 실패하면 결측 처리. (출처: [FRED DCOILWTICO](https://fred.stlouisfed.org/series/DCOILWTICO))

---

## 0. 프로젝트 개요

미국 주식시장의 폭락/조정 위험을 조기에 감지하는 **자동 모니터링 시스템**을 만들어주세요.

핵심 동작:
1. 외부 무료 API(**FRED 우선**, Yahoo Finance는 보조)에서 경제·시장 지표를 자동 수집
2. 각 지표를 신호등(green/yellow/orange/red)으로 자동 판정
3. 지표들을 조합해 **종합 판정**(평상시/경계/위험/최악 + **판정 보류**) 계산
4. 위험 신호가 뜨면 **텔레그램으로 휴대폰 알림** 발송 + **정상 작동 heartbeat**도 주기 발송
5. 결과를 날짜별 로그(CSV/JSON)로 저장
6. **GitHub Actions로 매일 자동 실행** (내 PC 꺼져 있어도 작동)
7. **GitHub Pages 무료 웹 대시보드**로 신호등을 폰·PC에서 시각적으로 확인

언어는 **Python**. 코딩을 잘 모르는 사용자이니, 각 단계마다 무엇을 하는지 쉽게 설명하고, 직접 해야 할 세팅(API 키 발급 등)은 명확한 클릭 가이드를 주세요.

---

## 1. 작업 진행 방식 (중요 — 반드시 지킬 것)

- **한 번에 다 만들지 말고 단계별로 진행.** 각 단계가 끝나면 멈추고, **"여기까지 됐는지 이렇게 테스트하세요"**를 알려준 뒤 사용자가 "다음"이라고 하면 진행.
- 직접 발급/설정해야 하는 것(FRED 키, 텔레그램 봇 토큰, GitHub repo, Pages 설정 등)은 **클릭 단위 가이드**로. 클릭할 버튼 이름·화면 위치를 구체적으로.
- 에러가 날 수 있는 부분은 미리 알려주고, 사용자가 에러 메시지를 붙여넣으면 고쳐주기.
- 코드에는 **한글 주석**을 충분히.
- **민감 정보 절대 코드에 직접 쓰지 말 것.** `.env` 파일 + 환경변수로 분리, `.gitignore`로 깃허브 노출 차단. README·예시에 토큰을 적을 땐 반드시 마스킹(`123456:ABC***` 형태).
- **`logging` 모듈 사용**(print 금지). Actions 로그에서 어느 단계에서 실패했는지 추적 가능하게.
- **모든 시간은 타임존을 명시**(naive datetime 금지). 시간 관련 처리는 `util.py` 한 곳에서.

---

## 2. 수집할 지표 및 판정 기준 (★ 소스 일부 변경)

판정 방향: **숫자가 높을수록 위험** (수익률곡선 2s10s만 예외 — 마이너스가 위험).

> **★ v2 소스 변경 강조:** VIX는 Yahoo→**FRED `VIXCLS`**. WTI는 **FRED `DCOILWTICO` 1순위 + Yahoo `CL=F` 폴백**. 그 결과 **Yahoo가 꼭 필요한 지표는 HYG·KRE 2개로 축소**(WTI는 폴백 시에만).

### Tier 1 — 신용시장 (가장 중요, "왕 지표")
| 지표 | 소스 | 코드 | 방향 | green | yellow | orange | red |
|------|------|------|------|-------|--------|--------|-----|
| 하이일드 스프레드 | FRED | `BAMLH0A0HYM2` | 높을수록 위험 | <4.0%p | 4.0~5.0 | 5.0~6.0 | ≥6.0 |
| 투자등급 스프레드 | FRED | `BAMLC0A0CM` | 높을수록 위험 | <1.5%p | 1.5~2.0 | 2.0~2.5 | ≥2.5 |
| 하이일드 ETF (HYG) | **Yahoo** | `HYG` | 추세 | 20일선 위 | 20일선 근처 | 20일선 아래 | 5%+ 하락 |
| 지역은행 ETF (KRE) | **Yahoo** | `KRE` | 추세 | 20일선 위 | 20일선 근처 | 20일선 아래 | 5%+ 하락 |

### Tier 2 — 매크로 (방향 결정)
| 지표 | 소스 | 코드 | green | yellow | red |
|------|------|------|-------|--------|-----|
| 근원 CPI (전년비) | FRED | `CPILFESL` (전년동월 % 계산) | 하락추세 | 보합 | 3개월 연속 상승 |
| 근원 PCE (전년비) | FRED | `PCEPILFE` (전년동월 % 계산) | 하락추세 | 보합 | 재가속 |
| 실업률 | FRED | `UNRATE` | 안정 | 상승 시작 | 0.5%p+ 급등 |
| 수익률곡선 2s10s | FRED | `T10Y2Y` | >0.2 | 0~0.2 | <0 (역전) |

### Tier 3 — 시장·심리 (단기 트리거)
| 지표 | 소스 | 코드 | green | yellow | orange | red |
|------|------|------|-------|--------|--------|-----|
| VIX 공포지수 | **FRED** | `VIXCLS` | <18 | 18~25 | 25~30 | ≥30 |
| WTI 유가 | **FRED `DCOILWTICO` → Yahoo `CL=F` 폴백** | (아래 참조) | <85 | 85~100 | 100~110 | ≥110 |
| 미 10년물 금리 | FRED | `DGS10` | <4.3 | 4.3~4.7 | 4.7~5.0 | ≥5.0 |

**WTI 수집 우선순위(코드 동작):**
1. FRED `DCOILWTICO` 호출 → 성공 & 신선도 OK(최근 N일 이내 값)면 채택
2. 값이 오래됐거나 실패면 Yahoo `CL=F`로 폴백
3. 둘 다 실패면 결측 처리(판정에서 제외, "수동 확인" 표시)

> **추세 판정(CPI/PCE):** "3개월 연속 상승"은 전년동월비(YoY = 현재값/12개월전값−1)를 직접 계산해야 합니다. FRED 원시값은 지수라 가공 필요. **복잡하면 일단 "전월 대비 상승/하락"으로 단순화하고, 주석으로 개선 여지(YoY 3개월 추세)를 남기세요.** 단, 단순화하면 red 판정이 거의 안 뜨는 한계가 있으니 README에 명시.

> **정성 지표(트럼프 발언 톤, 지정학 헤드라인)는 자동화하지 말고**, 리포트·대시보드에 "수동 확인 필요" 항목으로만 표시하세요.

> **주말·공휴일·결측:** FRED 일별 시리즈도 휴일엔 값이 없습니다. "오늘 데이터"를 고정으로 찾지 말고 **최신 유효값(last valid observation)을 받아 그 날짜를 함께 표기**하세요.

---

## 3. 종합 판정 로직 (★ 회색·보조경고·옵션 추가)

수집된 신호등을 조합해 종합 판정을 내립니다. **orange와 red만 "위험 점등"으로 카운트**(yellow는 경계지만 미카운트).

```
판정 규칙 (위에서부터 먼저 해당되는 것 적용):

0. [판정 보류 — 회색] ★신규
   결측 지표 수가 임계 초과 (config의 MAX_MISSING, 기본: 전체의 30% 또는 Tier1 4개 중 2개+)
   → ⚪ "판정 보류 — 데이터 부족"
   → 행동: 데이터 소스 점검 필요 / 이전 판정 참고 / 자동판정 신뢰 보류
   (★ 결측 다수를 🟢평상시로 오인하는 것을 막는 핵심 안전장치)

1. [최악] Tier1 위험 점등 1개+ AND Tier2 위험 점등 1개+
   → 🔴 "최악 진입 — 전면 방어"
   → 행동: 위험자산 최소화 / 현금·단기국채 최대 / 반등 추격매수 금지 / 신용시장 안정까지 대기

2. [위험] Tier1 위험 점등 1개+
   → 🟠 "위험 — 2차 방어"
   → 행동: 위험자산 절반 축소 / 현금 40~50% / ★빚(신용거래·미수) 즉시 청산★ / AI 집중 분산

3. [경계] Tier2+Tier3 위험 점등 합계 2개+
   → 🟡 "경계 — 1차 방어"
   → 행동: 신규매수 중단 / 현금 30% / 비싼 종목부터 일부 차익실현 / 헷지 소량

4. [평상시] 그 외
   → 🟢 "평상시 — 유지"
   → 행동: 정상 보유 / 현금 10~20%
```

**★ 보조 규칙(옵션 — config로 켜고 끄기):**
- **`YELLOW_WARN` (기본 ON, 임계 5):** 위험 점등 0개라도 yellow가 5개 이상이면 메시지에 "⚠️ 시장 전반 경계감 누적(yellow N개)" 한 줄 추가(판정 등급은 유지).
- **`TIER3_PANIC` (기본 OFF):** Tier3만 위험 점등 2개(예: VIX red + 유가 red)인 단기 패닉을 🟡경계로 끌어올림. 사용자가 단기 변동성에 민감하면 ON.

각 판정마다 위 행동 가이드 문구를 함께 출력/전송/대시보드 표시하세요. **이 옵션들은 모두 `config.py`에서 끄고 켤 수 있게.**

---

## 4. 알림 (텔레그램) — ★ heartbeat·에러 알림 추가

### 4-1. 위험 알림 (기존)
- 종합 판정이 🟠위험/🔴최악일 때 **즉시** 발송.
- 🟡경계는 하루 1회 요약. 🟢평상시는 발송 안 함(또는 주 1회 요약).
- ⚪판정 보류는 즉시 발송(데이터 점검 촉구).
- 내용: 종합 판정 + 점등 지표(현재 수치 포함) + 행동 가이드 + "수동 확인 필요" + 데이터 기준 날짜.

### 4-2. ★ heartbeat (정상 작동 통지) — 신규
- **"위험할 때만 알림"의 맹점**: 시스템이 죽어도 사용자는 평온합니다.
- 따라서 **주 1회(또는 매일, config로 선택) "✅ 시스템 정상 작동 중 — 현재 🟢평상시"** 한 줄을 보냅니다.
- config의 `HEARTBEAT_MODE`: `off` / `daily` / `weekly`(기본).

### 4-3. ★ 실패 알림 — 신규
- GitHub Actions 워크플로우 **자체가 실패**(파이썬 에러 등)하면 `if: failure()` 스텝에서 **별도 텔레그램 에러 메시지** 발송(아래 6단계 참조).
- 메시지: "🚨 모니터링 실행 실패 — Actions 로그 확인 필요 (시각·repo 링크)".

### 4-4. 봇 세팅
- @BotFather로 봇 만들기 → 토큰 받기 → chat_id 얻기를 **클릭 단위로** 안내.
- 토큰/chat_id는 `.env`(로컬) / GitHub Secrets(클라우드)에 저장. **코드·README엔 마스킹.**

예시 메시지:
```
🟠 위험 — 2차 방어 (데이터 기준: 2026-06-15)

[점등 지표]
🔴 하이일드 스프레드: 6.2%p (기준 6.0)
🟠 VIX: 27.3 (기준 25)

[행동]
• 위험자산 절반 축소, 현금 40~50%
• ★빚(신용거래) 즉시 청산★
• AI 집중 포지션 분산

[수동 확인 필요]
• 트럼프 내러티브 톤 / 이란·중동 헤드라인

[대시보드] https://<사용자>.github.io/market-signal/
```

---

## 5. 데이터 저장 (★ 신선도 검증 추가)

- 매 실행마다 결과를 `logs/signal_log.csv`에 한 줄 추가(append). **같은 날짜 중복 실행 시 덮어쓰기.**
- 컬럼: `날짜, 데이터기준날짜, 종합판정, Tier1점등수, Tier2점등수, Tier3점등수, 결측수, 각 지표 수치, 각 지표 신호등`.
- 최근 결과를 `logs/latest.json`에도 저장(웹 대시보드 연동용).
- **★ 데이터 신선도:** 각 지표의 관측 날짜를 함께 저장하고, **N일 이상 오래된 값엔 "오래된 데이터" 플래그.** 캐시된 옛값으로 잘못 판정하는 것 방지. (N은 config, 지표별로 다르게 — 일별 FRED는 5일, CPI/PCE 월별은 45일 등)
- **★ 웹 대시보드용 데이터:** `docs/data/latest.json`과 `docs/data/history.json`(추이 차트용, signal_log.csv를 가공)도 갱신(아래 8단계).

---

## 6. 자동 실행 (GitHub Actions) — ★ 7단계 세분화 + 수동 버튼 + 실패 알림

> **이 단계가 비개발자에게 가장 어렵습니다. 절대 한 번에 다 하지 말고 아래 7개 소단계로 쪼개세요.**

### 워크플로우 7단계 구조 (`.github/workflows/monitor.yml`)
1. **체크아웃**(repo 코드 가져오기)
2. **Python 설치**
3. **의존성 설치**(`pip install -r requirements.txt`)
4. **메인 실행**(`python main.py`) — Secrets를 환경변수로 주입
5. **로그 커밋**(아래 "로그 저장 권장안" 참조)
6. **웹 대시보드 데이터 갱신 + Pages 배포**(8단계에서 추가)
7. **★ 실패 시 텔레그램 에러 알림** — `if: failure()` 조건부 스텝

### ★ 수동 실행 버튼 먼저
- `on:` 트리거에 **`workflow_dispatch`(수동 실행 버튼)를 먼저** 넣으세요. cron은 **맨 마지막에** 추가.
- 이유: cron을 기다리지 않고 Actions 탭의 "Run workflow" 버튼으로 **즉시 1회 테스트** 가능. "왜 안 도는지" 며칠 기다리는 일 방지.

### ★ cron — KST↔UTC 환산표 (맨 마지막에 켜기)
GitHub cron은 **UTC 기준**입니다. "한국시간 아침 = 미국 장 마감 후"를 맞추려면 환산 필요.

| 원하는 한국시간(KST) | cron(UTC) | 비고 |
|---|---|---|
| KST 07:00 | `0 22 * * *` | 전날 UTC 22시 (KST = UTC+9) |
| KST 08:00 | `0 23 * * *` | |
| KST 09:00 | `0 0 * * *` | |

- **DST(미국 서머타임) 주의:** 미국 장 마감 시각이 1시간 움직입니다. 정확도가 중요치 않으니 **"장 마감 후 여유 있게"** 잡으세요(예: UTC 23시 = KST 08시 권장).
- **★ cron 함정 3가지(README에 명시):**
  - (a) cron은 best-effort — 정시 15~60분 지연 흔함, 드물게 스킵.
  - (b) **60일간 repo에 커밋 없으면 스케줄 자동 비활성화** → 이 시스템은 로그를 매 실행 커밋하므로 자연히 방지됨(또는 주 1회 더미 커밋).
  - (c) 지연/스킵 대비 heartbeat로 "이번 주 한 번도 안 왔다"를 사용자가 인지하게.

### ★ 로그 저장 권장안 (push 충돌·권한·히스토리 오염 방지)
**권장(기본):** Actions에서 CSV/JSON을 repo에 커밋.
- 워크플로우에 **`permissions: contents: write`** 명시(없으면 push 실패).
- push 전 **`git pull --rebase`** 후 push(연속 실행 충돌 방지).
- 커밋 메시지에 `[skip ci]` 넣어 재실행 루프 방지.

**대안(히스토리 오염이 싫으면):** logs/를 커밋하지 않고 **Actions artifact 업로드**(`actions/upload-artifact`).
- 단점: artifact 보존기간(기본 90일), 추이 차트용 누적 이력 유지가 번거로움.
- → **추이 차트(C 기능)를 쓰려면 커밋 방식이 더 적합.** 기본은 커밋 방식 채택.

### Secrets 등록 + repo 공개 여부 (★ 가이드 추가)
- API 키·텔레그램 토큰은 **GitHub Secrets**에 저장(코드 노출 금지). 등록을 클릭 단위로.
- **★ repo public vs private 선택 안내:**
  - **public**: Actions 무료 무제한 + GitHub Pages 무료. 단 **코드·logs/ CSV가 전부 공개**됩니다(토큰은 Secrets라 안전하지만 판정 이력·포지션 흐름이 노출). → **민감하면 logs를 커밋하지 않거나, 개인 판정 메모는 절대 코드에 안 넣기.**
  - **private**: 이력 비공개. 단 Actions 무료 분 한도(월 2,000분) 내 운영. **무료 GitHub Pages는 public repo 기준**이라, private면 Pages 사용에 제약 → **대시보드를 쓰려면 public 권장**.
  - **이 프로젝트 기본 권장: public repo + 민감 메모 비저장.** 신호등 이력 자체는 공개돼도 큰 문제 없는 시장 지표값입니다.

### 로컬 수동 테스트
- 내 PC에서 `python main.py`로 실행하는 법.
- 로컬 스케줄러(맥 cron / 윈도우 작업 스케줄러) 설정법도 간단히 안내(클라우드가 메인, 로컬은 백업).

---

## 7. ★ 웹 대시보드 (GitHub Pages 무료) — 신규

빌드 도구 없이 **단일 정적 페이지**(HTML/CSS/JS)로, 폰·PC 어디서나 URL로 신호등을 봅니다.

### 7-1. 동작 방식 (1~2줄 요약)
GitHub Actions가 매 실행마다 `docs/data/latest.json`·`docs/data/history.json`을 갱신 → `docs/index.html`이 그 JSON을 읽어 신호등·카드·차트를 그림 → GitHub Pages가 `docs/`를 자동 배포. **사용자는 `https://<사용자>.github.io/market-signal/`만 즐겨찾기하면 끝.**

### 7-2. 대시보드 구성 (`docs/index.html`)
- **종합 판정 — 큰 신호등**: ⚪🟢🟡🟠🔴 중 현재 상태를 크게(색·텍스트·행동 가이드).
- **Tier별 지표 카드**: 각 지표의 현재값 + 신호등 색 + 기준 날짜. Tier1/2/3 그룹으로.
- **행동 가이드**: 현재 판정의 행동 문구.
- **마지막 업데이트 시각**(KST 표기) + **데이터 기준 날짜**.
- **"수동 확인 필요"** 항목(트럼프 톤·지정학 헤드라인).
- **★ 과거 추이 라인 차트**(아래 7-3).
- 하단: **면책 문구** 고정.

### 7-3. ★ 과거 추이 그래프 (Chart.js CDN)
- `signal_log.csv` → `docs/data/history.json`으로 가공(날짜별 주요 지표값).
- **하이일드 스프레드·VIX** 등 주요 지표의 최근 추이를 **라인 차트**로.
- **차트 라이브러리는 CDN 한 줄로 쓰는 가벼운 것 — Chart.js 권장** (`<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>`). 빌드 불필요.
- 데이터가 1줄(첫 실행)이면 "추이 데이터 누적 중" 안내.

### 7-4. ★ GitHub Pages 켜는 법 (클릭 단위 — 사용자 직접)
> Claude Code는 아래 가이드를 사용자에게 그대로 안내하세요(무료·public repo 기준).

```
[GitHub Pages 켜기 — 클릭 가이드 자리]
1. GitHub에서 해당 repo 페이지로 이동
2. 상단 메뉴 [Settings] 클릭
3. 좌측 사이드바 [Pages] 클릭
4. "Build and deployment" → Source: [Deploy from a branch] 선택
5. Branch: [main] 선택, 폴더: [/docs] 선택 → [Save]
6. 잠시 후 상단에 "Your site is live at https://<사용자>.github.io/market-signal/" 표시
7. 그 URL을 폰 홈화면에 추가하면 앱처럼 사용
```
- (대안) Actions로 Pages 배포 시 `actions/deploy-pages` 사용 — 단 비개발자에겐 **branch+/docs 방식이 가장 단순**하므로 기본은 이 방식.

---

## 8. 지표 메타데이터 & config 강화 (★)

**임계값뿐 아니라 지표 메타데이터 전체를 `config.py` 한 테이블로** 모읍니다. 지표 추가/삭제를 코드 수정 없이.

각 지표 1개 = 1 레코드(예시 스키마):
```python
# config.py — 지표 메타데이터 (예시, 실제 구현은 Claude Code가)
INDICATORS = [
    {
        "name": "하이일드 스프레드",
        "source": "fred",            # fred | yahoo
        "code": "BAMLH0A0HYM2",
        "tier": 1,
        "direction": "high_bad",     # high_bad | low_bad | trend
        "thresholds": {"yellow": 4.0, "orange": 5.0, "red": 6.0},
        "stale_days": 5,             # 신선도: 며칠 넘으면 오래된 데이터
        "fallback": None,            # WTI는 {"source":"yahoo","code":"CL=F"}
    },
    # ... 나머지 지표 동일 패턴
]

# 종합 판정 옵션
MAX_MISSING = 0.30        # 결측 비율 임계 (초과 시 판정 보류)
YELLOW_WARN = {"on": True, "threshold": 5}
TIER3_PANIC = {"on": False}
HEARTBEAT_MODE = "weekly" # off | daily | weekly
```

`fetch.py`는 이 테이블을 읽어 source별로 분기 호출. **지표 추가/삭제 = 이 리스트만 수정.** README에 "지표 바꾸는 법"으로 정리.

---

## 9. 프로젝트 구조 (★ docs/·util.py·tests/ 반영)

```
market-signal/
├── main.py                 # 메인 실행 (수집→판정→알림→저장→대시보드 데이터 갱신)
├── fetch.py                # 데이터 수집 — fetch_fred() / fetch_yahoo() 함수 분리, 소스별 독립 try/except
├── signals.py              # 신호등 판정 + 종합 판정(회색·보조경고·옵션 포함)
├── notify.py               # 텔레그램 (위험 알림 + heartbeat)
├── config.py               # ★ 지표 메타데이터 테이블 + 임계값 + 옵션 한 곳에
├── util.py                 # ★ 타임존(ET/KST/UTC) 일원화, 날짜 비교, 신선도 검증, 로깅 설정
├── .env                    # API 키 (깃허브 제외)
├── .env.example            # 키 없는 템플릿
├── .gitignore              # .env, __pycache__ 등
├── requirements.txt        # fredapi/requests, yfinance, python-telegram(or requests), pytz 등
├── logs/
│   ├── signal_log.csv
│   └── latest.json
├── docs/                   # ★ GitHub Pages 웹 대시보드
│   ├── index.html          #   단일 페이지 (HTML+CSS+JS, Chart.js CDN)
│   └── data/
│       ├── latest.json     #   현재 판정·지표값 (대시보드가 읽음)
│       └── history.json    #   추이 차트용 (signal_log.csv 가공)
├── tests/                  # ★ 최소 단위 테스트
│   └── test_signals.py     #   임계값 경계(예: 정확히 6.0%p)·판정 보류 검증
├── .github/workflows/
│   └── monitor.yml         # 7단계 + workflow_dispatch + if:failure() 알림 + Pages 데이터 갱신
└── README.md               # 전체 사용법 + 고장 진단 체크리스트 + 면책
```

**임계값·메타데이터·옵션은 전부 `config.py` 한 곳.** 시간 처리는 전부 `util.py` 한 곳.

---

## 10. 단계별 진행 순서 (★ v2 — 이 순서로)

각 단계 끝에서 **"여기까지 됐는지 이렇게 테스트하세요"**를 알려주고 멈추세요.

> **전략(검토 의견 반영): FRED만으로 갈 수 있는 데까지 먼저 가서 "Yahoo 없이도 80% 작동하는 버전"을 만든 뒤, Yahoo를 별도 게이트로 추가.** Yahoo는 처음 들어오는 불안정 요소라 GitHub Actions 환경에서 1회 실측 후 넘어갑니다.

| 단계 | 내용 | 끝 테스트 |
|---|---|---|
| **1** | 폴더 구조 + `requirements.txt` + `.gitignore` + `util.py`(타임존·로깅 골격) 생성. 설치 명령 안내 | `python -c "import util; print(util.now_kst())"`가 타임존 붙은 시각 출력 |
| **2** | FRED 키 발급 가이드 → `fetch.py`로 **하이일드 스프레드 하나만** 수집·출력(최신 유효값+날짜) | 콘솔에 값·날짜 출력 |
| **3** | **나머지 FRED 지표 전부**(신용 2 + 매크로 4 + VIX `VIXCLS` + 금리 `DGS10` + WTI `DCOILWTICO`). 신선도 검증 포함 | FRED만으로 9개 지표 표 출력 |
| **4** | `signals.py` — 신호등 판정 + 종합 판정(★회색·보조경고·옵션). 콘솔에 보기 좋게 | 임계값 바꿔도 판정이 따라 바뀜 |
| **5** | **`tests/test_signals.py`** — 임계값 경계·판정 보류 단위 테스트 최소 1개 | `pytest`(또는 `python -m pytest`) 통과 |
| **6** | 로그 저장(`signal_log.csv`/`latest.json`) + 신선도 플래그 | logs 파일 생성·append 확인 |
| **7** | 텔레그램 봇 세팅 가이드 + `notify.py`(위험 알림 + heartbeat). 테스트 메시지 1회 | 폰으로 테스트 메시지 수신 |
| **8** | **Yahoo 추가(별도 게이트)** — HYG·KRE + WTI 폴백. retry+백오프+User-Agent+결측 처리 | 로컬에서 Yahoo 값 수신(실패해도 죽지 않음) |
| **9** | **웹 대시보드**(`docs/index.html` + `docs/data/*.json` + Chart.js 추이). 로컬에서 브라우저로 열어 확인 | 브라우저에서 신호등·카드·차트 표시 |
| **10** | **GitHub Actions(7-a~7-f 소단계)** — repo 생성 → push → Secrets → workflow 파일 → **`workflow_dispatch` 수동 1회 실행** → (Actions에서 Yahoo 실측 확인) → cron 마지막 활성화 | Actions 탭에서 수동 실행 성공 + 텔레그램 수신 |
| **11** | **GitHub Pages 켜기**(Settings→Pages, /docs) + 대시보드 자동 갱신 확인 | 폰에서 `github.io` URL 접속 성공 |
| **12** | `README.md`(지표 바꾸는 법 + KST↔UTC 표 + **고장 진단 체크리스트** + 면책) + 전체 점검 | README만 보고 6개월 뒤 혼자 진단 가능 |

**10단계(Actions) 소단계:** 7-a repo 생성 → 7-b 로컬 push → 7-c Secrets 등록 → 7-d workflow 파일 → 7-e **`workflow_dispatch`로 수동 1회 실행(여기서 Actions 환경의 Yahoo 차단 여부 실측)** → 7-f cron 한 줄 추가.

---

## 11. 추가 요청 (기술 디테일)

- **FRED**: `fredapi` 또는 직접 `requests` 중 안정적인 쪽. **Yahoo**: `yfinance`. 단 Yahoo 호출엔 **재시도(2~3회)+지수 백오프+User-Agent 헤더** 적용.
- **데이터 못 가져오면** 프로그램이 죽지 않고 "데이터 없음(결측)"으로 처리, 알림·대시보드에 표시. **소스별 try/except 독립**(Yahoo 실패가 FRED 결과를 날리지 않게).
- **타임존**: `util.py`에서 ET/KST/UTC 명시적 처리. naive datetime 금지.
- **로깅**: `logging` 모듈. Actions 로그에서 단계별 추적.
- **무료 범위**에서만 동작(유료 API 불필요).
- 지표·임계값·옵션 바꾸는 법을 README에 정리(전부 `config.py`).

---

## 12. ★ 고장 진단 체크리스트 (README에 넣을 것)

알림이 안 올 때 비개발자가 혼자 점검:
1. GitHub **Actions 탭** → 최근 실행이 성공(초록 체크)인가? 실패(빨강 X)인가?
2. 실패면 → 어느 스텝에서? (체크아웃/설치/실행/커밋/Pages 중)
3. 실행은 됐는데 알림이 없다면 → 판정이 🟢평상시라 안 보낸 것일 수 있음(heartbeat 마지막 수신 확인).
4. **Yahoo 결측인가 FRED 결측인가?** (대시보드의 "오래된 데이터/결측" 플래그 확인) — Yahoo면 일시 차단 가능성, 잠시 후 자동 복구.
5. 텔레그램 토큰 만료/변경? → 봇에게 직접 메시지 보내 chat_id 재확인.
6. cron이 안 돈다 → 60일 무커밋 비활성화 여부 확인(Actions 탭 상단 경고), `workflow_dispatch`로 수동 재가동.

---

## 13. 면책 (코드 주석·README·대시보드 하단에 모두 넣을 것)

이 시스템은 투자 판단을 돕는 **보조 모니터링 도구**이며, 투자 조언이나 매매 신호가 아닙니다. 모든 투자 결정과 결과의 책임은 사용자 본인에게 있습니다. 데이터는 외부 무료 API에 의존하므로 지연·오류·결측 가능성이 있으며, 자동 판정이 시장을 완전히 대표하지 않습니다.

---

**자, 1단계부터 시작해줘.**
