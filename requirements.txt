# ─────────────────────────────────────────────────────────────
# 이 프로젝트가 필요로 하는 외부 파이썬 라이브러리 목록입니다.
# "pip install -r requirements.txt" 한 줄로 아래 것들이 한꺼번에 설치됩니다.
# (각 줄 끝의 # 뒤는 설명용 주석이라 컴퓨터는 무시합니다)
# ─────────────────────────────────────────────────────────────

requests        # FRED 등 인터넷에서 데이터를 받아오는 도구 (가장 기본)
yfinance        # Yahoo Finance 데이터 수집 (8단계에서 사용 — HYG·KRE·WTI 폴백)
tzdata          # 윈도우에서 '뉴욕/서울' 같은 표준시간대를 인식하게 해주는 데이터
python-dotenv   # .env 파일에 적은 API 키를 코드가 안전하게 읽게 해주는 도구
pytest          # 5단계에서 만들 자동 테스트 실행 도구
